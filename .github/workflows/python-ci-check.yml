---
name: Lint and scan python code

on:
  push:
    branches: [ "main" ]
    paths: [ "app-code/**", ".github/workflows/python-ci-check.yml" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-22.04
    steps:
      # - name: Check out main
      #   uses: actions/checkout@main
      - name: Install pylint
        run: pip3 install pylint==2.15.2
      - name: List dirs
        run: ls /home/runner/work/naviteq_private/app-code/
      - name: Run linter
        run: pylint /home/runner/work/naviteq_private/app-code/app.py
      # - name: GitHub Action for pylint
      #   uses: cclauss/GitHub-Action-for-pylint@0.7.0
  scan:
    runs-on: ubuntu-22.04
    steps:
      - name: Check out main
        uses: actions/checkout@main
      - name: Security vulnerabilities scan
        env:
          DEP_PATH: app-code/requirements.txt
        uses: aufdenpunkt/python-safety-check@master
...
