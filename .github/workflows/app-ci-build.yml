
---
name: Build image

on:
  push:
    tags: [ "v[0-9].[0-9]+.[0-9]" ]
    paths:
      - 'app-code/**'
      - '.github/workflows/app-*'
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      print_tags:
        description: 'True to print to STDOUT'
        required: true
        type: boolean
      tags:
        description: 'Image tag'
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v3
    - name: print manual tags
      if: ${{ inputs.print_tags }}
      run: echo  The tags are ${{ inputs.tags }}
    - name: Set tag
      env:
        MANUAL_TAG: ${{inputs.tags }}
      run: |
        if [[ $MANUAL_TAG ]]
        then
          echo "TAG=${{ inputs.tags }}" >> $GITHUB_ENV
        else
          echo "TAG=${GITHUB_REF_NAME}" >> $GITHUB_ENV
        fi
    - name: Build the Docker image
      run: docker build app-code/ --file app-code/Dockerfile --tag czm41k/weather_checker:${{ env.TAG }}

## TODO - upload image job
...
